export const MCP_SDK_META = {
	sdk: '@siteline/cloudflare-mcp-worker',
	sdkVersion: '1.0.0',
	integrationType: 'cloudflare-mcp-worker',
} as const;

export const DEFAULT_MCP_TRANSPORT = 'streamable-http';

# Skip build scripts for native dependencies
# This prevents issues with sharp/node-gyp on newer Node.js versions
# Safe for Cloudflare Workers since native modules aren't used at runtime
ignore-scripts=true
